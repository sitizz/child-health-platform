# Audio Analysis (Roadmap)

Planned microphone-based ML capability:

- **Cough sound classification** — dry / wet / allergic cues with caregiver guidance.

Status: API contract only (`POST /api/v1/ml/audio/analyze` → HTTP 501).

Audio ML will augment — never replace — clinical assessment.
